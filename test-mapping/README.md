# Test Mappings

Shift-Left test mappings generated by the `test-mapping` skill. Each mapping is a flat, per-file table of every test in a scoped set, with SHA-pinned links back to the source so rows never drift.

Scope here is **by owning team**, not by repo — a team's tests are selected via the `OWNERSHIP.md` files in `Zocdoc/sandbox` under `playwright/BU/`.

| Mapping | Team | Source | Tests |
|---------|------|--------|-------|
| [sandbox-auth-test-mapping.md](sandbox-auth-test-mapping.md) | Infrastructure / Auth (`#auth`, Jira `AUTH`) | `Zocdoc/sandbox` @ `89a501e` · `playwright/BU/Infrastructure/Auth` | 6 files / 24 tests |

Version history: [CHANGELOG.md](CHANGELOG.md).
